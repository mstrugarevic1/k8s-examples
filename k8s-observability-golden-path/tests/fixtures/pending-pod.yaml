apiVersion: v1
kind: Pod
metadata:
  name: pending-too-large
  namespace: observability-fixtures
  labels:
    app: pending-too-large
spec:
  containers:
    - name: pending-too-large
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]
      resources:
        requests:
          cpu: "1000"
          memory: 1Ti

